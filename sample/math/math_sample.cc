#include <cstdio>
#include <math/math.h>

int main()
{
    //math::mat2x2 c = math::mat2x2::identity();
    //printf("[%f, %f]\n", c[0][0], c[0][1]);
    //printf("[%f, %f]\n", c[1][0], c[1][1]);

    //math::mat2x3 a;
    //a[0][0] = 0;
    //a[0][1] = 3;
    //a[0][2] = 5;
    //a[1][0] = 5;
    //a[1][1] = 5;
    //a[1][2] = 2;

    //math::vec3 b(3, 4, 3);

    //math::vec2 c = a * b; // M*v
    //printf("[%f, %f]\n", c.x, c.y);
    //printf("should be [27, 41]\n");
    
    //math::mat3x3 a;
    //a[0][0] = 2;
    //a[0][1] = 1;
    //a[0][2] = 3;
    //a[1][0] = 3;
    //a[1][1] = 3;
    //a[1][2] = 2;
    //a[2][0] = 4;
    //a[2][1] = 1;
    //a[2][2] = 2;

    //math::vec3 b(1, 2, 3);

    //math::vec3 c = b * a; //v*M
    //printf("[%f, %f, %f]\n", c.x, c.y, c.z);
    //printf("should be [20, 10, 13]\n");

    return 0;
}
